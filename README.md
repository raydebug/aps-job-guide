# APS 找工指导网站

一个面向 APS 背景求职者的静态指南网站，包含找工路线图、岗位方向库、简历写法、投递节奏和面试准备。

## 本地预览

```bash
npm install
npm run dev
```

## 发布到 GitHub Pages

1. 在 GitHub 新建仓库。
2. 把本项目推送到 `main` 分支。
3. 在仓库设置中打开 `Pages`，将发布来源设为 `GitHub Actions`。
4. 每次推送到 `main` 后会自动部署。
